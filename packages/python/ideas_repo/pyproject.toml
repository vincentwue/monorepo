[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ideas_repo"
version = "0.1.0"
description = "Domain logic and persistence helpers for ideas hierarchy."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "db_core",
    "permissions",
]
